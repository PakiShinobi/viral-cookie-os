import { PodcastHubClient } from "./podcast-hub-client";

export const metadata = {
  title: "Podcast Studio · Viral Cookie OS",
};

export default function PodcastHubPage() {
  return <PodcastHubClient />;
}
